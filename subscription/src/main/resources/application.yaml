server:
  port: 8070
spring:
  application:
    name: subscription
  main:
    banner-mode: off
  datasource:
    url: jdbc:mysql://localhost:3306/vilka_subscription
    username: vilka_mvp
    password: vilka_mvp
  flyway:
    enabled: true
    locations: classpath:db/migration
  jwt:
    secret: ${JWT_SECRET}

services:
  identity:
    url: http://localhost:8080
  marketplace:
    url: http://localhost:9000

#logging:
#  level:
#    root: ERROR
#    org.springframework: OFF
#    org.hibernate: OFF
#    com.vilka.app.subscription: INFO
#    org.flywaydb: DEBUG

