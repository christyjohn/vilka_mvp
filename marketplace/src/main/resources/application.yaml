server:
  port: 9000
spring:
  application:
    name: catalog
  main:
    banner-mode: off
  datasource:
    url: jdbc:mysql://localhost:3306/vilka_marketplace
    username: vilka_mvp
    password: vilka_mvp
  flyway:
    enabled: true
    locations: classpath:db/migration
  jwt:
    secret: ${JWT_SECRET}

services:
  vendor:
    url: http://localhost:8090

#logging:
#  level:
#    root: ERROR
#    org.springframework: OFF
#    org.hibernate: OFF
#    com.vilka.app.catalog: INFO
#    org.flywaydb: DEBUG

