package com.vilka.app.vendor.entity;

//TODO - Currrently this is redundant as we only have VENDOR scenario
// later we could actually use this to create different types of vendors
public enum Role {
    USER,
    VENDOR,
    ADMIN
}
