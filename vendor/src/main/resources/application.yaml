server:
  port: 8090
spring:
  application:
    name: vendor
  main:
    banner-mode: off
  datasource:
    url: jdbc:mysql://localhost:3306/vilka_vendor
    username: vilka_mvp
    password: vilka_mvp
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    root: ERROR
    org.springframework: OFF
    org.hibernate: OFF
    com.vilka.app.vendor: INFO
    org.flywaydb: DEBUG

services:
  identity:
    url: http://localhost:8080